# Slack (Bot token + signing secret)
SLACK_BOT_TOKEN=xoxb-...
SLACK_SIGNING_SECRET=...
PORT=3000
# Optional: If you want to index channels across multiple workspaces/installs later,
# you will need to persist installation info. This starter assumes a single workspace.

# Postgres
DATABASE_URL=postgres://postgres:postgres@localhost:5432/slack_rag

# Ollama
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_EMBED_MODEL=nomic-embed-text
OLLAMA_CHAT_MODEL=llama3.1

# RAG
TOP_K=8
MAX_CONTEXT_CHARS=12000

# Indexer
SYNC_INTERVAL_SECONDS=300
HISTORY_PAGE_LIMIT=200
MAX_MESSAGES_PER_WINDOW=20
MAX_WINDOW_MINUTES=10
